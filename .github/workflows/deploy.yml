# DEPRECATED: This workflow has been replaced by ci.yml
# The new CI/CD pipeline includes comprehensive testing, security scanning, and safe deployment
# Please use .github/workflows/ci.yml instead

name: Legacy Deploy (Deprecated)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Legacy Deploy (Not Recommended)
    runs-on: ubuntu-latest
    if: false  # Disable this workflow

    steps:
      - name: Warning
        run: |
          echo "⚠️  This workflow is deprecated and disabled."
          echo "🔄 Please use the new CI/CD pipeline in .github/workflows/ci.yml"
          echo "📋 The new pipeline includes:"
          echo "   - Comprehensive testing (102 tests)"
          echo "   - Code quality checks (ruff, black, mypy)"
          echo "   - Security scanning (bandit, safety, trivy)"
          echo "   - Safe deployment with health checks"
          echo "   - Automatic rollback on failure"
